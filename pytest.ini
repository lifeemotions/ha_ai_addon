[pytest]
pythonpath = lifeemotions_ai_addon
testpaths = tests
asyncio_mode = auto
markers =
    integration: tests that require Docker (HA container)
    slow: tests that take a long time (HA Supervisor startup)
